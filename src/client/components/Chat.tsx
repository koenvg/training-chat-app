import * as React from 'react';

interface Props {
  messages: string;
}

export class Chat extends React.PureComponent<Props, {}> {
  render() {
    return (
      <div>
        test
      </div>
    );
  }
}